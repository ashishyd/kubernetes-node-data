apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: host-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  volumeName: host-pv
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
